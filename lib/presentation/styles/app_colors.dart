import 'package:flutter/cupertino.dart';

class AppColors {
  const AppColors._();

  static const Color primaryColor = Color(0xFFD13406);


  static const LinearGradient gradient = LinearGradient(
    colors: [
      Color.fromRGBO(41, 55, 72, 0.6),
      Color.fromRGBO(0, 0, 0, 0),
    ],
  );
}
